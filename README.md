# lerobot-setup
This repository contains a script to setup lerobot in a raspberrypi.

Run the below command in the terminal of the raspberrypi to setup lerobot with teleop along with tmux.
```bash
curl -fsSL https://raw.githubusercontent.com/manugovindv/lerobot-setup/master/setup_lerobot.sh | bash
```
